import 'package:flutter/material.dart';

class InvalidScreen extends StatelessWidget {
  const InvalidScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Invalid Scren'),
      ),
    );
  }
}
